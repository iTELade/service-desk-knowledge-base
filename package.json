{
  "name": "itelade-service-desk-knowledge-base",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "description": "Self-hosted Confluence-like knowledge platform for iTELade Service Desk and standalone documentation.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
